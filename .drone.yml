kind: pipeline
name: default

steps:
- name: docker  
  image: plugins/docker
  settings:
    username: docker-username
    password: docker-secret
    repo: pereirafmauro/chopp-ti
    dockerfile: Docker/
    tags:
    - latest
